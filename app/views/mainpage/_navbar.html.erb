
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-brand"><%= link_to "WTB", root_path, :class => "navbar-link" %></p>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right ">
        <% if current_user %>
        <li class="navbar-user"><%= "Hello, " + @current_user.name.split(" ")[0] + "!" %></li>
        <li class="dropdown">
          <a class="dropdown-toggle fa fa-train" data-toggle="dropdown" href="#"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Edit User Info", edit_user_path(@current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "logout", logout_path %></li>
          </ul>
        </li>
        <% else %>
        <li><%= link_to "sign-up", new_user_path %></li>
        <li><%= link_to "log-in", login_path %></li>
        <li class="dropdown">
          <a class="dropdown-toggle fa fa-train" data-toggle="dropdown" href="#"><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li></li>

          </ul>
          <% end %>
        </ul>
      </li>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
