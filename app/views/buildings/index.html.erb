<!-- Splash page -->

<%= render 'mainpage/jumbotron' %>

<%= render 'mainpage/map' %>

<%= render 'mainpage/cards' %>

<% @buildings.each do |b| %>
  <%= link_to b.name, building_path(b) %>
<% end %>
